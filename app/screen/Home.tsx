import React from "react";
import { Text, View } from "react-native";

const Home: React.FC<{navigation}> =({navigation}) => {
  return(
    <View>
      <Text>home</Text>
    </View>
  );
};

export default Home;
